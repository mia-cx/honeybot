apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "honeybot.fullname" . }}-config
  labels:
    {{- include "honeybot.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
