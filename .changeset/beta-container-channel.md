---
"honeybot": minor
---

Add deterministic beta container releases from `main` and immutable stable container releases from Changesets version tags, with idempotent Docker Hub and GHCR reconciliation.
